# Contact form (Resend) — copy to .env.local and fill in
RESEND_API_KEY=
# Optional overrides — defaults are trenton@shyft.dev and Resend's onboarding
# sender. Set CONTACT_FROM_EMAIL to an address on shyft.dev once the domain is
# verified in Resend.
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=